```
━━ ⏸ PLAN 待确认 · L 档 · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  折扣率最多允许 4 位小数，超过就报错
  不改金额精度（仍保留 2 位），不存折扣率

我替你做的决定
  1. 小数位去掉末尾 0 后再计数，0.12340 算 4 位，接受
       备选：按原始写法计数，0.12340 拒绝
  2. core 和接口两层都加校验
     接口用 @Digits(fraction=4)，超限返回 400
       备选：只改 core，但接口会返回 500
  3. 折扣率不存库，不用迁移，幂等逻辑不改

需要你选
  需求说"目前最多 2 位"，但代码现在不限小数位
  （现有单测已在用 0.678、0.3333）
  所以真正的改动是新加 4 位上限
  以前 5 位以上能成功（201），以后会返回 400
  按"新加上限"做吗？我倾向是

验收标准（4 条）
  1. 0.1235 算出 1.24，0.12340 算出 1.23
  2. 0.12345 和 0.000001 在 core 报错，区间校验不变
  3. 接口传 0.12345 返回 400，传 0.1235 返回 201
  4. 接口用 0.1235 下单，响应和库里金额都是 1.24

任务（3 条）
  1. core 加小数位校验和单测
  2. 接口加 @Digits 并补测试（依赖 1）
  3. 集成测试验证落库金额（依赖 1、2）

完整计划  docs/changes/2026-09-18--discount-rate-four-decimals/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
备注  项目还没初始化，建议跑 /viktor-init；这轮检查命令从 README 推出来：`./mvnw -q test`，集成测试用 `./mvnw -q verify`
