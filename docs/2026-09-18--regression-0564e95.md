# 修复后回归结果（fe-ai-workflow @ 0564e95）

> 2026-09-18，Claude Code 2.1.276 无头会话（`claude -p`），Codex CLI 0.155.0（`codex exec`）。脚本：`viktor-validation/run3-lib.sh`（公用函数，含 `cx` Codex 驱动）、`run3-46.sh`（4.6）。日志：`viktor-validation/logs/r3/`（下文简写 `r3/`），作废运行放在 `r3/void/`。`git status` 证据记在 `logs/git-status.log` 里以 `r3` 开头的条目。
> 对照基线：`docs/2026-09-18--backend-validation.md`（两轮，P1～P11）。规格：`docs/2026-09-18--validation-fixes.md` 第六节。
> fe-ai-workflow 除本文件外没有改动；回归中发现的问题只记录，没有修。没有 push 任何仓库。

## 结论速览

- **通过**：R3-1 init 重跑、R3-2 S 档、R3-3 blocked 卡片、R3-6 两条清理路径、R3-8 自测。
- **通过（有偏差）**：R3-4 manual → ship（F7 已关闭，但第一次 check 被误判为 blocked，见 P12）、R3-5 noinit（主体符合；"撤销信任"子项在 noinit 上无法触发，换到有放行规则的仓库补测后通过，见 P15）。
- **部分通过**：R3-7 Codex（不提权 ✅、danger-full-access 被拒 ✅；卡片不合模板；**三个合法沙箱值里没有能跑 Maven 的**，可行配置在 AGENTS.md 里写不进去，见 P17）；R3-9 todolist F5（差异 1、2 消失；差异 3 只在重跑 init 后部分消失，见 P13、P14）。
- **新问题**：P12～P21。影响最大的是 P12（macOS 上 check 探测会误判 blocked）、P13（knowledge.sh 放行规则和提示词里的路径对不上）、P14（升级后不重跑 init，第一次 flow 就在 review 报 error）。

## 0. 升级

| 仓库 | 提交 | 核对 |
|---|---|---|
| springboot-demo | `300ac03 chore: upgrade fe-ai-workflow to 0564e95` | `.claude/skills/viktor-check/SKILL.md` 含"CHECK 需要处理"卡模板；AGENTS.md 只有 F9 的 L 档判据一行变了 |
| springboot-demo-noinit | `08e0f48`（同上） | 同上；settings.json 仍然只有 hook，没有放行规则（第二轮的状态） |
| todolist-demo `f5-rerun` | `241b8e4`（同上） | 同上 |
| todolist-demo `f5-rerun-2`（从 `f129c8c` 新建） | `9bf10eb`（同上） | 同上 |

install 日志：`r3/r3-0-install-*.log`。三个仓库在 `~/.claude.json` 里都是 `hasTrustDialogAccepted: true`。

## 1. 场景结果

| # | 场景 | 期望 | 实际 | 结论 | 证据 |
|---|---|---|---|---|---|
| R3-1 | springboot-demo 重跑 `/viktor-init`（重复执行模式） | settings.json 放行 knowledge.sh、`docker ps/rm/run/stop`、dev；节点卡"检查命令"一行只用 ✅ / — / 未验证；AGENTS.md 其他内容没被改坏 | 第一轮回复只列了差异、没有改文件：①"命令验证"一行的执行数过时（5/7 → 17/25）；②缺 6 条放行。回复"按探测结果更新"后：settings.json 新增 `Bash(bash .workflow/fe-ai-workflow/scripts/knowledge.sh:*)`、`Bash(./mvnw -pl app -am spring-boot:run)`、`Bash(docker ps:*)`、`docker rm`、`docker run`、`docker stop` 共 6 条，原有 3 条保留，一共 9 条；AGENTS.md 的 diff 只有"命令验证"这一行；节点卡为 `typecheck — lint — test ✅ verify ✅ e2e — dev 未验证`。偏差：第一轮的差异说明是**英文**（P19） | **通过** | `r3/r3-1-init*.final.txt`；`r3/r3-1-{AGENTS,settings}.{before,after}.*`；demo `ec60c0d` |
| R3-2 | S 档一次 | plan.md frontmatter 没有模板注释；`.review.log`、`.check.log` 为 stream-json；review / check 自动跑完 | **第一次**（金额上限 100 万）被判成 **M**，理由是"历史超限订单用同一个幂等键重放要不要仍然返回 201"，这是真实存在的取舍，判档成立。回复 OK 后 review pass、check pass，自动 ship。**补跑一次真正的 S 档**（改 quantity 的异常文案），全程自动完成。两次的 plan.md frontmatter 都没有行尾注释；两份日志都是 stream-json（例如 62 行 / 37 行，事件类型有 system / assistant / user / result），`result` 事件里带 `permission_denials`，从而查到了 P9 的根因（P18） | **通过** | `r3/r3-2-S*.final.txt`、`r3/r3-2b-S.final.txt`、`r3/r3-2b-plan.md`；demo `docs/changes/2026-09-18--amount-upper-limit/`（`9c77fff`）、`…--quantity-error-message/`（`014875a`） |
| R3-3 | 4.1 blocked（停 Docker，plan 退回 review，`/viktor-check`） | 退出码 4；不改代码；卡片和 F6 模板逐字一致 | spawn 输出 `EXIT=4`；check.md 为 `result: blocked`，pending 列了 AC-2、AC-3 两条关键 AC 和原因；`git diff --stat` 只涉及需求目录下的 4 个文件。卡片：标题行 `━━ ⚠ CHECK 需要处理 · M · 1m ━━━━…` 与模板同长；"关键 AC 证据缺失，未改代码："、`详情  docs/changes/…/check.md`、最后一行"环境处理好后说「继续」"都与模板一致。唯一的差别是底部分隔线 50 个 ━（模板 58 个），不影响阅读。**作废运行一次**：驱动脚本没加载 `fm_set`，plan 没有退回，主会话正确回复"没有可验收的需求"（`r3/r3-3-void-nofmset.*`） | **通过** | `r3/r3-3-blocked.final.txt`、`r3-3-check.md`、`r3-3-diffstat.txt`；demo `3fb89cb`（前置）、`2fc6470`（结果）、`2dbf478`（还原） |
| R3-4 | 4.3 manual 走完 ship（AC-9 在 plan 里；plan 退回 review；`/viktor-flow` 续接） | check `manual`、`pending: [AC-9]`；report.md 的"待人工确认"**只有** AC-9；review 的 SUGGESTED 都在"剩余 SUGGESTED"；状态条"待人工 1 项" | **第一次**：代码指纹变了，先复审（pass），然后派 check。check 子进程用 `timeout 20 docker ps >/dev/null 2>&1 && echo DOCKER_OK \|\| echo DOCKER_FAIL` 探测，macOS 没有 `timeout` 命令，得到 DOCKER_FAIL；按提示词"探测失败不重试"没有再试，也没有跑 verify，判 **blocked**（EXIT=4），实际上 Docker 是好的（P12）。卡片符合 F6，卡片之后多了一行"备注"。**同一会话回复"继续"后**：check `manual`、`pending: [AC-9]`，自动 ship；report.md 的"待人工确认"只有 AC-9 一行，并附人工步骤；第二轮混进待人工的 `Order.java:14`（旧库升级）这次和其余 4 条一起进了"剩余 SUGGESTED"；状态条"check ✅ 6 👀 1 ❌ 0 · 待人工 1 项"；`verified.pending: [AC-9]` | **通过（F7 已关闭）**；首次 check 误判记为 P12 | `r3/r3-4-first-*`（误判）、`r3/r3-4-manual-2.final.txt`、`r3/r3-4-{check,report,plan}.md`；demo `c30cfe0`（第一次）、`3a53433`（结果）、`a59c5d6`（plan 还原） |
| R3-5 | 4.5 noinit S 档（被权限拦住） | 预检里完整验收写在 `verify:` 键，不误用 `e2e:`；review `error`、退出码 2；卡片为 F6 的 error 卡，最后一行含 /viktor-init；AGENTS.md / settings.json 的 sha 不变 | 先把第二轮停在 review error 的需求归档（`status: archived`），避免续接冲突。需求换成"quantity 上限 999"（"4 位小数"那条在第二轮已经落进代码了）。判成 **M**（第二轮同类需求判 L，F9 生效），回复 OK。预检块：`test: ./mvnw -q test`、`verify: ./mvnw -q verify`，**没有 `e2e:`**；README 里有 `spring-boot:run`，但没列 `dev`（P20）。review 派单用 `--checks`，子进程的 `./mvnw -q test` 和 `knowledge.sh lookup` 都被拒，`result: error`，spawn 输出 `exit=2`；plan 为 `stage: review`、`stage_result: error`。卡片标题和最后一行"运行 /viktor-init 补齐权限；你自己放行了就说「继续」"都对；**第一段写成了"审查进程没有执行检查命令的权限，本轮审查没有给出结论："**，不是模板里的"检查命令无法执行："，标题写"第 1/3 轮"（review_round 仍为 0），卡片后多了一行备注（P16）。sha 前后一致 | **通过（卡片措辞有偏差）** | `r3/r3-5-*`（`r3-5-sha-before.txt` / `r3-5-sha-after-run.txt`、`r3-5-plan.draft.md`、`r3-5-review.error.md`）；noinit `ed87474` |
| R3-5+ | 撤销 noinit 的信任再派 review | spawn 退出 2，stderr 含"未被信任" | 备份 `~/.claude.json`（`backup/claude.json.r3-5.bak`），删掉 noinit 的 `hasTrustDialogAccepted`，直接调 spawn 派 review：**退出 2，但 stderr 没有"未被信任"**，只有通用的"review 无法执行"。原因：Claude Code 只有在确实要忽略 allow 规则时才打印 `Ignoring N permissions.allow entries from .claude/settings.json: this workspace has not been trusted…`，noinit 没有任何 allow 规则，所以没有这句话，F2 无从识别（P15）。**补测**：在有 9 条规则的 springboot-demo 上同样撤销信任再派 review：`EXIT=2`，stderr 为"工作区未被 Claude Code 信任（…）；请在项目目录（…/springboot-demo）交互式启动一次 claude 并选择信任，然后说「继续」…"。两个目录的信任随后都已还原为 `true`，需求目录用 `git checkout` 还原 | noinit 上**未触发**（由检测机制决定，属于期望设计问题）；F2 本身**通过** | `r3/r3-5u-*`（noinit）、`r3/r3-5b-*`（demo 补测） |
| R3-6a | 4.6 手工登记（`VIKTOR_SPAWN_TIMEOUT=40`） | 名字含 run_id 并已登记的容器，超时后被删 | spawn 启动后读出 run_id `20260918215329-50854`，起 `viktor-20260918215329-50854-pg` 并写入 `.check.resources`。`EXIT=2`（"check 超时（40s）"）；`docker events` 显示这个容器在 spawn 结束的同一秒（`1789739651`）被 kill / destroy；`docker ps -a` 为空；`claude -p` / surefire / failsafe / maven / java 都没有残留 | **通过** | `r3/r3-6-a-manual.*`（`docker-events.txt`、`docker-at-timeout.txt`、`ps-after.txt`）。脚本在 bash 3.2 下有一次空数组报错，那次作废运行在 `r3/void/` |
| R3-6b | 4.6 Testcontainers 兜底 | 不登记；超时时 IT 容器还在，被 F5 兜底删掉；派单前已存在的容器不动；没有残留 | IT 的 Postgres 容器正常只活 2 秒，40 秒超时很难正好碰上，所以临时在 OrderApiIT 里加了 `@BeforeAll Thread.sleep(90s)`（`ee4da8b`，事后 revert 为 `530a27e`）；`--checks` 只给 `verify: ./mvnw -q -pl app -am verify`。派单前先起了 `r3-6-pre-pg`，**并给它加上 `org.testcontainers.sessionId` 标签**，比没有标签的普通容器更严格。结果：超时前 3 秒快照里本轮的 TC 容器 `beautiful_napier` 还在；`EXIT=2`；这个容器在 spawn 结束的同一秒（`1789739725`）被 destroy，比 ryuk 自行退出（`…734`）早 9 秒，说明是 F5 删的；stderr 报"未处理（本轮开始前已存在的 Testcontainers 容器）：72fe9d533efd"，`r3-6-pre-pg` 一直是 Up 状态；ryuk 容器本身没有 sessionId 标签，10 秒后自己退出；没有残留进程 | **通过** | `r3/r3-6-b-tc.*` |
| R3-7 | Codex S 档（折扣率最多 2 位小数） | 默认沙箱下 review 报 error → 需要处理卡，不改 `VIKTOR_CODEX_ARGS`、不提权；AGENTS.md 子进程参数找出能跑 Maven 的最小值；`danger-full-access` 被拒（退出 2） | 判 S。code 完成后 spawn 派 `codex exec --sandbox workspace-write`：Mockito 报 "Could not self-attach"，app 11 个 errors，`result: error`，`exit 2`。主会话**没有**改参数重跑：jsonl 里 `danger-full-access` 只出现在它 `cat` spawn 源码的输出里，没有出现在任何执行过的命令里。它还引用了 SKILL 里"不得修改沙箱或权限参数"这句话。**但卡片不合模板**：用了自造字段"实现 / 测试 / 阻塞 / 进度 / 下一步"，标题写成"S 档"，没有"检查命令无法执行："这一段（P16）。**沙箱值试验**（直接调 spawn）：`codex exec --sandbox` 只有 read-only / workspace-write / danger-full-access 三个值。① `danger-full-access` → 立即拒绝，`EXIT=2`，原因写清楚了 ✅；② `read-only` → 子进程连 review.md 都写不了，spawn 报 run_id 不符，`EXIT=2`；③ workspace-write 就是默认值，已知跑不了。**所以三个合法值里没有能跑 Maven 的。**根因定位：用 `codex sandbox` 在仓库副本里直接跑（不经过模型），`workspace-write` 失败，**`workspace-write` 加上 `-c sandbox_workspace_write.network_access=true`** 后 `./mvnw -q test` 和 `verify`（OrderApiIT 8 个，真实 Postgres）都通过。Mockito self-attach 需要本地 socket，被沙箱的网络限制拦下了。④ 这个配置写进 AGENTS.md → spawn 报"只支持 'codex --sandbox <值>'"，`EXIT=2`；⑤ 经 `VIKTOR_CODEX_ARGS` 传入 → 不被 F1 拒绝，review `pass`（`./mvnw -q test` 25 个）、check `pass`（`./mvnw -q verify`），`EXIT=0`。README 没改 | **部分通过**：不提权、拒绝提权两项通过；卡片不合模板；"配置项目级沙箱值后通过"在现有格式下**做不到**（P17，需要用户决定） | `r3/r3-7-d1-codex.*`（主会话）、`r3/r3-7-{dfa,fmt,ro,wwnet,wwnet-check}.*`、`r3/r3-7-sbx-*.log`（`codex sandbox` 直接执行）；`r3/void/r3-7-d1-stdin-hang.*`（第一次没关 stdin，`codex exec` 卡在 "Reading additional input from stdin"，作废）；demo `a934b94` |
| R3-8 | 工作流自测 | 四个脚本全绿 | `validate.sh` 45 通过 / 0 失败；`spawn.test.sh`、`install.test.sh`、`knowledge.test.sh` 均 PASS；跑完后 fe-ai-workflow 工作区干净 | **通过** | `r3/r3-8-*.log` |
| R3-9 | todolist-demo F5 重跑（`f5-rerun-2`，从 `f129c8c` 开始，升级后跑同一需求，回复 OK） | 第一轮差异 1（报告的待人工与 check 不一致）、2（启动 dev 被拒）、3（knowledge.sh 被拒）消失；"关键"列全是"否"；附加项"未涉及" | **按交接原样跑**：plan 判 M，7 条 AC；回复 OK 后 code 完成，review 子进程的 `knowledge.sh lookup` 被拒，按 F3 判 **`result: error`**（第一轮是退回去读 index.md，这次不绕了），flow 停在 REVIEW 需要处理卡（格式合规，最后一行提示 /viktor-init）。原因：升级只换了技能，没有补放行规则，todolist 的 settings.json 仍是旧 init 写的 5 条（P14）。**按卡片提示**运行 `/viktor-init`，回复"按探测结果更新"：新增 knowledge.sh、`npm run dev` 两条放行，AGENTS.md 加了"命令验证"一行和 `### 运行前提`，删了过时的"没有 commit"，并**把 `.claude/` 从禁区里拿掉了**（P21）。然后在 flow 会话里回复"继续"：review 第 2 轮 pass → check pass → ship。对照：**差异 1 消失**（check `pass`、`pending: []`，报告"待人工 0 项"；没在浏览器里看过的外观放在卡片后的备注里，没有算进待人工）；**差异 2 消失**（check 子进程 `npm run dev` 启动成功，pid 登记进 `.check.resources`，用完就停了；浏览器工具 `browser_navigate` 被拒，放弃了浏览器这条路）；**差异 3 部分残留**：review 子进程第一次按提示词用**绝对路径** `bash /Users/…/todolist-demo/.workflow/fe-ai-workflow/scripts/knowledge.sh lookup …` 调用，仍然被拒，换成相对路径后才通过（P13）；差异 4（plan.md 模板注释）消失。check.md 的"关键"列 7 条都是"否"；review 附加项为"未涉及迁移 / 鉴权 / 幂等 / 外部调用 / 查询"。另外交付回复的第一句是英文（P19） | **部分通过**（1、2 消失；3 必须先重跑 init，重跑后仍有绝对路径被拒的问题） | `r3/r3-9-*`（`r3-9-init.diff`、`r3-9-F5-run.final.txt` 为停在 review 时、`r3-9-F5-cont.final.txt` 为完成后）；todolist `f5-rerun-2`：`a4e8fb7`（init）、`8891161`（F5） |

## 2. 未关闭或新发现的问题

对照第二轮：P1（F2）在有放行规则时关闭，没有规则时见 P15；P2（F4）新 init 已补齐，但见 P13、P14；P3（F6）的 Claude check blocked 卡已合规，review error 卡和 Codex 仍有偏差（P16）；P4（F7）关闭；P5（F5）关闭；P6（F1）的"不提权"关闭，可行沙箱值见 P17；P7（F9）本轮两个同类需求都判 M，按预期；P8（F8）`verify:` 键和 plan.md 注释已关闭，节点卡标记已关闭，`dev` 未列见 P20；P9 根因见 P18；P10（F3）本轮没有看到替换命令，R3-9 的 review 严格按 F3 判 error；P11（F4）关闭。

| # | 问题 | 表现场景 | 原因判断 |
|---|---|---|---|
| P12 | **macOS 上 check 的环境探测可能误判 blocked**：子进程用 `timeout 20 docker ps >/dev/null 2>&1 && echo OK \|\| echo FAIL` 探测，macOS 默认没有 `timeout`，"command not found" 被 `2>&1` 吞掉，结果被当成 Docker 不可用；按提示词"任一探测失败就放弃该路径——不重试"，verify 没跑，直接判 blocked（EXIT=4） | R3-4 第一次；第二轮 `46-A`、`46-B` 也用了同样的写法（当时后面又跑了 mvnw，没有暴露出来） | 提示词加环境问题：`prompts/check.md` 要求"探测不超过 1 分钟"和"失败不重试"，模型就套上了 GNU `timeout`。建议在提示词里写明探测命令直接执行（例如 `docker ps`），不要包 `timeout`、不要丢弃 stderr，"命令不存在"不算环境不可用。spawn 本身已经有超时兜底 |
| P13 | **knowledge.sh 的放行规则是相对路径，提示词里却是绝对路径**：init 写的是 `Bash(bash .workflow/fe-ai-workflow/scripts/knowledge.sh:*)`，spawn 把 `{{WORKFLOW_DIR}}` 替换成绝对路径（`cd … && pwd`），子进程照提示词第一次调用 `bash /Users/…/.workflow/fe-ai-workflow/scripts/knowledge.sh lookup …`，被拒 | R3-2（demo，review）、R3-9（todolist，review 第 2 轮） | 脚本和 init 规则不一致。这两次子进程都自己改用相对路径成功了，但如果严格按 F3"只执行给定命令"，就可能判 error。可选的修法：spawn 把 WORKFLOW_DIR 替换成相对于项目根目录的路径；或者 init 同时放行绝对路径的写法 |
| P14 | **升级之后放行规则不会自动补齐**：install / upgrade 只更新技能，老项目的 settings.json 没有 knowledge.sh / dev / docker。F3 之后子进程不再绕过，第一次 flow 就在 review 报 error | R3-9 | 流程或文档缺口：CHANGELOG / upgrade.sh 的输出应该提示"升级到 1.1.0 后请重跑 /viktor-init（重复执行模式）补齐放行规则"。也可以让 upgrade 检测 settings.json 缺少 knowledge.sh 规则时给出警告 |
| P15 | **F2 的未信任检测依赖 Claude Code 的一条警告**（`Ignoring N permissions.allow entries … has not been trusted`），这条警告只在 settings.json 里有 allow 规则时才会打印；项目没有规则时，未信任和未初始化的表现完全一样，spawn 只报通用的 error | R3-5+（noinit） | 检测方式的局限，不算回归：这种情况下 error 卡提示运行 /viktor-init，init 之后如果仍未信任，下一次派单就能识别出来（demo 补测已证明）。如果希望一步到位，spawn 可以在派单前直接读 `~/.claude.json` 里 `projects[<pwd>].hasTrustDialogAccepted`。交接里"noinit 上 stderr 含'未被信任'"这条期望要修正 |
| P16 | **需要处理卡仍有自由发挥**：① Codex 主会话完全没用模板（字段是"实现 / 测试 / 阻塞 / 进度"）；② Claude 的 review error 卡第一段写成了"审查进程没有执行检查命令的权限…"，不是"检查命令无法执行："，而且 error 时标题写"第 1/3 轮"（review_round 为 0）；③ 卡片后面经常跟一行"备注"（R3-4 第一次、R3-5、R3-9）。check 的 blocked 卡（R3-3）完全合规 | R3-7、R3-5、R3-4、R3-9 | 提示词遵从度问题。"只输出这张卡"对卡片后面的备注没有约束力；Codex 对模板的遵从明显更弱。可以考虑在 SKILL 里写明"卡片之外不输出任何文字"，并在 viktor-review 里说明 error 时轮次写 review_round（不加 1） |
| P17 | **Codex + JVM 没有可用的项目级沙箱值**：`--sandbox` 只有三个值，read-only 更窄，workspace-write 跑不了 Mockito，danger-full-access 被 F1 拒绝。实测可行的最小配置是 `--sandbox workspace-write -c sandbox_workspace_write.network_access=true`，但 AGENTS.md 的"子进程参数"只接受 `--sandbox <值>`；这个配置只能通过 `VIKTOR_CODEX_ARGS` 传入，而 F1 并不拒绝它 | R3-7 | 规格和格式问题，需要用户决定（见第三节）。注意：`network_access=true` 同时放开了子进程的外网访问 |
| P18 | **组合命令会被逐段审批**（P9 的根因，stream-json 里可以看到）：`./mvnw -q verify > /tmp/x.log 2>&1; echo EXIT=$?`、`cmd 2>&1 \| tail; echo "EXIT=${pipestatus[1]}"`、`cat …; bash knowledge.sh …` 都被拒（"Contains simple_expansion"、"multiple operations"），单独执行 `./mvnw -q verify` 就能通过。子进程大多会自己退回到单条命令，但会浪费轮次，而且和 P12 的"不重试"有冲突 | R3-2、R3-4、R3-9 | 放行规则 `Bash(./mvnw:*)` 只能匹配单条命令，属于 Claude Code 的正常行为。建议 `prompts/review.md` / `check.md` 写明：检查命令单独执行，不加管道、重定向、`;` 和 `$?`，需要结果时去读 surefire / failsafe 报告 |
| P19 | 回复语言漂移：init 重复执行模式的差异说明、R3-9 交付前的第一句都是英文，其余节点卡是中文 | R3-1、R3-9 | 模型波动，影响小。SKILL 可以加一句"用中文回复" |
| P20 | 预检没有列 `dev`：README 里明确有 `./mvnw -pl app -am spring-boot:run`，F8 要求"可推导的键列全" | R3-5 | 提示词遵从度问题，影响小（本轮 AC 不需要运行中的服务） |
| P21 | init 重复执行模式顺手缩小了禁区：把 `.claude/`、`.agents/` 改成只写 `.agents/`，理由是"settings.json 的 permissions 由 init 维护"。但 `.claude/skills`、`.claude/hooks` 仍然由 install.sh 管理 | R3-9 | init 的"差异"范围太宽，把用户写的约定也算了进去。这次是先提出、经确认后才改的，没有越权；建议 init 只对"项目信息"里探测得到的字段提差异，不改禁区和约定 |

## 3. 需要用户决定

1. **Codex + JVM 的沙箱值**（P17）。实测结果：
   - `--sandbox read-only`：跑不了（连报告都写不了）。
   - `--sandbox workspace-write`（默认）：Mockito self-attach 失败。
   - `--sandbox danger-full-access`：F1 拒绝，符合设计。
   - `--sandbox workspace-write -c sandbox_workspace_write.network_access=true`：`./mvnw -q test` 和 `verify`（Testcontainers + 真实 Postgres）都通过，review / check 经 spawn 跑出 pass。代价是子进程可以访问外网。
   
   可选方案：(a) 放宽 AGENTS.md"子进程参数"的格式，允许在 `--sandbox workspace-write` 后面加 `-c sandbox_workspace_write.network_access=true`（仍然拒绝 danger-full-access），README 写明 JVM 项目用这个配置；(b) 不改格式，README 说明 JVM 项目用 `VIKTOR_CODEX_ARGS` 传入；(c) 项目侧把 Mockito 配成 surefire 的 `-javaagent`，避免 self-attach（本轮**没有验证**，而且 Testcontainers 在没有网络权限时能否连上 docker socket 也没测）；(d) 声明 Codex 端暂不支持 JVM 项目的独立验收。建议 (a)。
2. **是否发 v1.1.0 并 push**：F1～F10 在回归里基本成立（P4、P5、P11 关闭，P1、P3、P6、P8 大部分关闭）。但 P12（macOS 上 check 可能误判 blocked）、P13（knowledge.sh 路径）、P14（升级后要重跑 init）会让 Mac 用户和升级用户第一次运行就碰到问题。建议先修 P12～P14（改动都在提示词、spawn 的路径替换、CHANGELOG / upgrade 提示），再做一次小回归（R3-4、R3-9 各跑一次），然后发版。如果要马上发，至少在 CHANGELOG 里写明"升级后重跑 /viktor-init"。push 由你执行。
3. P15 的处理方式：接受现状（没有放行规则时由 /viktor-init 引导），还是让 spawn 在派单前读 `~/.claude.json` 主动判断。
4. P16 / P21 是否要进 v1.1.0，还是留到下一版。
5. 验证仓库的处理：本轮在 springboot-demo（`300ac03`〜`014875a`）、noinit（`08e0f48`〜`ed87474`）、todolist `f5-rerun`（`241b8e4`）/ `f5-rerun-2`（`9bf10eb`〜`8891161`）上的提交都没有 push。`~/.claude.json` 的两份备份在 `viktor-validation/backup/claude.json.r3-5*.bak`，信任状态已经还原。

## 附：本轮对验证仓库的改动

- springboot-demo：`300ac03` 升级；`ec60c0d` init 重跑；`9c77fff` R3-2；`3fb89cb` / `2fc6470` / `2dbf478` 4.1；`14b5475` / `c30cfe0` / `3a53433` / `a59c5d6` 4.3；`ee4da8b` / `530a27e` 4.6b 的临时 sleep 和它的 revert；`7a5ddc1` / `a934b94` Codex；`014875a` S 档补跑。4.6a / 4.6b 对需求目录下 dotfile 的改动都用 `git checkout` 还原了。
- springboot-demo-noinit：`08e0f48` 升级；归档第二轮的需求；`ed87474` R3-5 结果（停在 review error）。
- todolist-demo：`f5-rerun` 上 `241b8e4` 升级；新分支 `f5-rerun-2`：`9bf10eb` 升级、`a4e8fb7` init 重跑、`8891161` F5 重跑。
