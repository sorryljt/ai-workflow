```
━━ ✅ 交付 · 订单创建接口支持幂等键 · L · 合计 8m ━━━━━━━━━━━━
改动 9 文件 +281 −8 · 测试 22 passed · review 1 轮 · check ✅ 6 👀 0 ❌ 0 · 待人工 1 项
```

## 待人工确认
| # | 项 | 怎么验 | 期望 |
|---|----|--------|------|
| 1 | 已有数据的库升级（IT 用的是空库） | 对已有 `orders` 表的库执行 `./mvnw -pl app -am spring-boot:run`，再在 psql 里执行 `\d orders` | 出现 `idempotency_key`、`request_hash` 两列和唯一约束 `uk_orders_idempotency_key`；历史行两列都为 NULL |

## 本轮做了什么
| 项 | 做了什么 | 改动文件 | 验证 | 结果 |
|----|---------|---------|------|------|
| T1 契约 | orders 表新增两列和唯一约束；新增 `findByIdempotencyKey`、`OrderCreation`，以及 400 / 422 两个异常 | Order.java、OrderRepository.java 等 | 编译 | ✅ |
| AC-1 | 带键首次创建时写入键和 SHA-256 指纹 | OrderService.java | OrderApiIT 1 例（真实 Postgres） | ✅ |
| AC-2 | 同键同体重放首单：201，带 `Idempotent-Replayed: true` | OrderService.java、OrderController.java | IT 1 例 + WebMvc 1 例 | ✅ |
| AC-3 | 并发时靠唯一约束兜底，冲突后按键回查 | OrderService.java | IT 8 线程并发 1 例 | ✅ |
| AC-4 | 同键不同体返回 422 `IDEMPOTENCY_KEY_REUSED` | OrderService.java、OrderController.java | IT 1 例 + WebMvc 1 例 | ✅ |
| AC-5 | 不带键行为不变；键不同但 orderNo 重复仍返回 409 | OrderController.java | IT 原有 3 例 + 新增 1 例 | ✅ |
| AC-6 | 键不合法时返回 400，不进入 service | OrderController.java | WebMvc 3 例 | ✅ |

## 审查记录
| 轮 | 结果 | BLOCKING | 处理 |
|----|------|----------|------|
| 1 | pass | 0 | — |

剩余 SUGGESTED：
- Order.java:14 —— 在已有旧 schema 的库上确认 ddl-auto 能建出约束（已列入待人工确认第 1 条）
- Order.java:37 —— `request_hash` 实际是 varchar(64)，plan 里写的是 char(64)，两边要统一
- OrderService.java:59 —— 给 `fingerprint` 补 surefire 单测
- OrderApiIT.java:106 —— review 本轮没拿到 verify 证据（check 已补上，OrderApiIT 8/8 通过）

## 耗时
| plan | code | review | check | ship | 合计 |
|------|------|--------|-------|------|------|
| 1m（不含等待确认） | 3m | 2m | 1m | 1m | 8m |

## 沉淀的知识
- decisions：订单幂等键存在 orders 表上，不建独立幂等记录表
- pitfalls：OrderService.create 不能加外层 @Transactional，否则并发幂等回查失效
- glossary：Idempotency-Key 语义：全局唯一、永久有效、只有创建成功才占用

## 建议的 commit message
```
feat: 订单创建接口支持 Idempotency-Key 幂等键（viktor L 档）

- orders 表新增 idempotency_key（唯一）与 request_hash 列
- 同键同体返回首单（201 + Idempotent-Replayed: true），同键不同体返回 422
- 并发由唯一约束兜底，冲突后按键回查
- 非法键返回 400；不带键时行为不变
```

备注：未自动 commit；review 派单脚本把 review 相关的新文件以 intent-to-add 状态加进了索引，check 的产物和知识库新文件仍是未跟踪状态。
