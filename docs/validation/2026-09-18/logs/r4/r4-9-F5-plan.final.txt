```
▶ M 档  plan（等你确认）→ code → review → check → ship
```

```
━━ ⏸ PLAN 待确认 · M · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  顶部搜索框，输入即按文字不区分大小写子串过滤，
  与状态筛选、优先级排序叠加；不持久化、不高亮

我替你做的决定
  1. 搜索词先 trim，只输入空格视为没有搜索词
       备选：原样匹配，空格也算字符
  2. 全选 / 剩余 N 项 / 清除已完成仍作用于全部待办
       备选：只作用于当前搜索结果
  3. 无搜索词且无待办时仍显示「暂无待办」
  4. 搜索框放在标题下、新增表单上方，不包在 form 里
  5. 搜索中新增了不匹配的待办，不自动清空搜索词

验收标准（7 条）
  1. 输入 milk / MILK 实时只剩 Buy Milk，清空后恢复
  2. 搜索 + 未完成 / 已完成筛选叠加
  3. 搜索 + 优先级排序叠加，分组标题随结果变化
  4. 无匹配显示「没有匹配的待办」
  5. 不写 localStorage，重新挂载后搜索框为空
  6. 只输入空格时显示全部
  7. 全选 / 计数 / 清除已完成不受搜索影响

完整计划  docs/changes/2026-09-18--search-filter-3/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
