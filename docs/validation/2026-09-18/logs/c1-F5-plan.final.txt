```
━━ ⏸ PLAN 待确认 · M · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  顶部搜索框，输入即过滤，不区分大小写子串匹配，
  与状态筛选、优先级排序叠加。
  不做：持久化、提交按钮、高亮、模糊匹配

我替你做的决定
  1. 搜索框放在标题下、新增表单上方
       备选：放进筛选工具栏同一排
  2. 搜索词先去首尾空格；只有空格时不过滤
       备选：原样匹配，不去空格
  3. 全选、剩余计数、清除已完成仍作用于全部待办
       备选：只作用于搜索后可见的条目
  4. 匹配项被状态筛选挡掉也显示「没有匹配的待办」
  5. 只改 App.tsx 和 App.css，不改 useTodos 和存储

验收标准（7 条）
  1. 输入 milk 或 BUY 立即只剩 Buy Milk，无提交按钮
  2. 搜索加状态筛选叠加生效
  3. 搜索加优先级排序叠加，只出现有匹配的分组标题
  4. 有搜索词无匹配时显示「没有匹配的待办」，清空后恢复
  5. 无搜索词或只有空格时，空态仍是「暂无待办」
  6. 搜索词不进 localStorage，刷新后清空
  7. 剩余计数、全选、清除已完成不受搜索影响

完整计划  docs/changes/2026-09-18--search-filter/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
