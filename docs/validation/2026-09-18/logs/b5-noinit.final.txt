```
━━ ⏸ PLAN 待确认 · L · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  折扣率由 (0, 1] 放宽为 [0, 1]，0 为免单，金额 0.00
  不做：免单审批或权限、免单标识、历史数据处理

我替你做的决定
  1. core 和 POST /orders 参数校验一起放宽
       备选：只改 core，接口仍对 0 返回 400
  2. 按 L 档走：接口契约变了（传 0 由 400 变为 201）
  3. amount 列没有大于 0 的约束，不做迁移
  4. 幂等指纹已把 0 / 0.00 归一，不改
  5. 负数、大于 1、null 仍报错，接口层返回 400

验收标准（4 条）
  1. core 计算：折扣率 0 或 0.00 得 0.00，scale 为 2
  2. core：-0.01 / 1.01 抛异常，1 仍为原价
  3. POST 传 0：返回 201，响应和库里金额都是 0.00
  4. POST 传 -0.01 / 1.01：返回 400，不落库

任务（3 条）
  1. core 校验与单测
  2. 接口 DTO 校验与 WebMvc 用例（依赖 1）
  3. OrderApiIT 免单落库（依赖 1、2）

完整计划  docs/changes/2026-09-18--allow-zero-discount/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
备注：项目还没初始化，建议跑一次 /viktor-init；这次的检查命令从 README 推导，已写入 plan 的「本轮运行配置」
