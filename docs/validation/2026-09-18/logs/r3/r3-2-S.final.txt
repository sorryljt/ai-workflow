```
━━ ⏸ PLAN 待确认 · M · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  金额计算结果超过 1000000.00 时抛 IllegalArgumentException
  不改请求体校验、HTTP 状态码映射和历史数据

我替你做的决定
  1. 舍入后再比较：1000000.00 放行，1000000.01 拒绝
       备选：用舍入前的原始值比较
  2. OrderService 改成幂等键回查未命中后才算金额，
     这样历史超限订单重放仍返回 201（按已记录的坑）
       备选：不挪，历史超限订单重放会变成 500
  3. 超限走默认 500，跟 quantity 上限保持一致
       备选：加 400 handler（已有接口的状态码会跟着变）
  4. 上限写死为常量 MAX_TOTAL，不做成配置

验收标准（3 条）
  1. 计算器边界：1000000.00 放行，.01 拒绝，
     .004 放行，.005 拒绝，原有用例全部通过
  2. 超限请求不返回 201，真实 Postgres 里不落库
  3. 历史超限带键订单：同键同体重放 201，同键不同体 422

完整计划  docs/changes/2026-09-18--amount-upper-limit/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
