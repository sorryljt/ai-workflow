```
━━ ⏸ PLAN 待确认 · M · 1m ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

目标
  顶部加搜索框，输入即做不区分大小写的子串过滤，
  与状态筛选、优先级排序叠加。不持久化、不高亮、
  不做模糊匹配

我替你做的决定
  1. 搜索词先 trim，只输入空白视为没有搜索词
       备选：原样匹配，含空格
  2. 搜索框放在标题下、新增表单上方，type=search
  3. 过滤顺序：状态筛选 → 搜索 → 优先级排序分组
  4. 有搜索词且结果为空一律显示「没有匹配的待办」，
     包括状态筛选本身已为空的情况
  5. 剩余 N 项、全选、清除已完成仍按全部待办计算
  6. 搜索词只放在 App 的 useState，不经过 useTodos

验收标准（7 条）
  1. 输入 milk 只剩 Buy Milk，无提交按钮
  2. 与未完成 / 已完成筛选叠加
  3. 与排序叠加，段标题只出现在有结果的优先级上
  4. 无匹配显示「没有匹配的待办」，清空后恢复
  5. 不写 localStorage，重新挂载后搜索框为空
  6. 只输入空白时不过滤
  7. 剩余计数、全选、清除已完成不受搜索影响

完整计划  docs/changes/2026-09-18--search-filter-2/plan.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
没问题回 OK，有要改的直接告诉我
```
